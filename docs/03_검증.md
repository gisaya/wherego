# 검증

최종 갱신: 2026-07-12 KST

## 현재 검증

최근 결과:

- 2026-07-08 21:57 KST: 아래 약관 정적 빌드 명령 성공.
- 2026-07-08 22:05 KST: `.env.local` Git 제외 확인, 커밋 대상 인증키 패턴 검사 통과, 아래 약관 정적 빌드 명령 성공.
- 2026-07-08 22:16 KST: Vercel production 배포 후 아래 URL 모두 HTTP 200 확인.
- 2026-07-09 KST: 질문풀 기반 Gemini 대체 플로우에서 한국관광공사 국문 관광정보 API와 지역별 방문자수 API 호출 성공.
- 2026-07-09 KST: 서울시청 테스트 위치 기준, 왕복 2시간 조건을 추정 도로거리/주행시간으로 필터링하고 주차 정보 확인 후보 3곳을 추천하는 것 확인.
- 2026-07-09 KST 저장 검증: 약관 정적 빌드 성공, `scripts/probe-question-bank-result.cjs` 문법 검사 통과, 질문풀 구조 13그룹/390문항/그룹당 30문항 확인, 커밋 후보에서 64자 hex 인증키 패턴 없음 확인.
- 2026-07-09 KST 질문지 보완 검증: 질문 생성기와 추천 프로브 문법 검사 통과, 원천질문 축별 2/4지선다 구성 확인, 일반질문 태그 그룹 13개/390문항/그룹당 원천 선택지 12개 확인, 실제 API 프로브 성공.
- 2026-07-09 KST 질문 카드 목업 검증: 이미지 없는 카드형 2지선다/4지선다 화면, 8문항 클릭 흐름, 리워드 게이트까지 브라우저에서 확인.
- 2026-07-09 KST 앱 저장 검증: 약관 정적 빌드, `scripts/probe-question-bank-result.cjs --check`, `yarn typecheck`, `git diff --check`, `yarn build` 성공. 앱인토스 산출물 `wherego.ait` deploymentId `019f4666-aafa-7a02-b955-c802dffc027d` 생성 확인. 결과 화면 상단 헤더 숨김과 목업 SVG 카드 다운로드 `C:\Users\ESOL\Downloads\wherego-일월수목원.svg` 생성 확인.
- 2026-07-09 KST 카드 저장 수정 검증: SVG 파일 저장 대신 `react-native-svg` `toDataURL` 기반 PNG 저장으로 변경. `yarn typecheck`, `git diff --check`, `yarn build` 성공. 앱인토스 산출물 `wherego.ait` deploymentId `019f4675-4c16-7bf2-b6a2-caef2741f1f7` 생성 확인.
- 2026-07-10 KST 저장 검증: 첫 화면 문구 블록을 앱과 목업 모두 중앙 쪽으로 내림. 약관 정적 빌드, `scripts/probe-question-bank-result.cjs --check`, `yarn typecheck`, `git diff --check`, `yarn build` 성공. 앱인토스 산출물 `wherego.ait` deploymentId `019f4916-8162-7022-8180-1e354788acc6` 생성 확인.
- 2026-07-10 KST 실기기 dev 확인: Android 무선 디버깅에서 `adb reverse tcp:8081 tcp:8081` 후 `intoss://wherego` 실행 성공. 로컬 번들 URL이 HTTP 200을 반환했고, Apps in Toss 샌드박스에서 첫 화면, 출발 선택 화면, 현재 위치 기반 1/8 질문 화면 진입을 확인.
- 2026-07-10 KST 결과 카드 조정: 저장 PNG 카드 상단 실제 관광지 사진 영역을 키우고, 위치/AI 선택 근거 영역을 압축했다. AI 선택 근거 문구의 앞쪽 불릿/점 찌꺼기 정리 로직을 추가했다.
- 2026-07-10 KST 저장 검증: `jbg` 백엔드에 `POST /api/wherego/questions`를 추가하고 앱이 서버 질문 세트를 우선 사용하도록 변경했다. SDK 확인 전 UI로 질문지 생성 로딩 화면, TDS Button loading, 선택지 번호 고정 배지, 긴 문구 2줄 제한을 반영했다. `jbg` Wherego route unittest 16개, `wherego` `yarn typecheck`, `git diff --check`, `yarn build` 성공. 앱인토스 산출물 `wherego.ait` deploymentId `019f4a16-6e2b-7383-a0b6-6d42ff134754` 생성 확인.
- 2026-07-10 KST 저장 검증: 질문지 생성 로딩 화면부터 배너 광고를 노출하고, 질문 세트 로딩을 최소 2초 유지하며, 배너를 질문지 생성/각 질문 화면마다 remount하도록 변경했다. `wherego` `yarn typecheck`, `git diff --check`, `yarn build` 성공. 앱인토스 산출물 `wherego.ait` deploymentId `019f4a1f-9496-78cf-8f75-9181ee47d010` 생성 확인.

- 2026-07-10 KST 저장 검증: 저장 PNG 카드 사진 영역을 추가 확대하고 위치 문구를 주소 중심으로 단순화했다. 약관 정적 빌드, `scripts/probe-question-bank-result.cjs --check`, `yarn typecheck`, `git diff --check`, `yarn build` 성공. 앱인토스 산출물 `wherego.ait` deploymentId `019f49fb-2216-7512-b555-4808770c2e41` 생성 확인.
- 2026-07-10 KST 저장 검증: 추천 흐름을 무료 후보 준비(`/api/wherego/candidates`)와 Gemini 최종 선택(`/api/wherego/recommend`)으로 분리했다. 앱은 리워드 광고 CTA에서 후보 준비를 시작하고, `userEarnedReward` 이후에만 Gemini를 호출한다. 광고 완료 뒤 전용 AI 로딩 화면을 보여준다. `jbg` Wherego route unittest 18개, `wherego` `yarn typecheck`, `git diff --check`, `yarn build` 성공. 앱인토스 산출물 `wherego.ait` deploymentId `019f4a38-eefa-7f99-a0b0-217c6cb4363b` 생성 확인.
- 2026-07-11 KST 저장 검증: 리워드 광고를 배너와 겹치지 않도록 리워드 게이트 진입 후 로드하도록 변경했다. 출시 소스에는 라이브 광고 그룹 ID만 사용하고, 15초 로드 타임아웃·재시도·SDK 이벤트 로그·동기 예외 처리를 추가했다. Render 실서비스에서 8문항, 후보 4개, `source.curator=gemini`, 최종 이미지 URL을 확인했다. `jbg` unittest 20개, 약관 빌드, 질문 검증, `yarn typecheck`, `git diff --check`, `yarn build` 성공. 앱인토스 산출물 deploymentId는 `019f5096-2949-7302-a914-4fbf34166f12`이다.
- 2026-07-12 KST 저장 검증: 질문 흐름을 원천 3개 + 일반 4개의 총 7문항으로 줄이고, 이지선다에서 의미가 겹치는 선택지 조합을 생성기와 서버 양쪽에서 제외했다. 전면광고 이벤트 누락/실패 시 8초 안에 추천을 계속하는 보호 로직을 추가했다. 관광공사/DataLab 후보는 최대 18개로 정리한 뒤 최종 서버 점수와 검색 의도 다양성에 따라 5~7개만 Gemini에 전달하고, 최종 1개만 상세·이미지를 조회한다. `jbg` Wherego unittest 43개와 Python 컴파일, `wherego` TypeScript 검사, 약관 빌드, 질문 스크립트 문법 검사, 질문은행 해시 일치, 이미지 규격, `git diff --check`, AIT build가 성공했다. 앱인토스 산출물 deploymentId는 `019f54d0-abe0-768b-b02c-326006f0b471`이다. 업로드용 최종 로고 후보 `assets/wherego-logo-600.png`는 600x600이며 코드/URL에는 연결하지 않았다.
- 2026-07-12 KST 배포 후 검증: Render health가 `jbg` 커밋 `dca1eae8b8a7cf32700880ea777ec3193838184c`를 반환했다. 운영 7문항 조합 3개에서 원천/일반 `3/4`, 후보 축소 `19 -> 7 -> 5`, `97 -> 12 -> 5`, `144 -> 18 -> 5`를 확인했다. Gemini 최종 응답은 `curator=gemini`, 모델 `gemini-3.1-flash-lite`, 추천 1개 `고양어린이박물관`, 이미지 포함으로 성공했다. 별도 `59 -> 12 -> 5` 조합은 유효 검색 의도가 2개라 의도별 2개와 와일드카드 1개로 5개가 된 정상 가변 압축이었다.
- 2026-07-12 KST 저장 검증: 결과 화면과 저장 PNG 카드에서 관광지 사진 우측 하단을 가리던 검은 출처 배지를 제거하고, 사진 아래의 작은 회색 출처 캡션으로 이동했다. 한국관광공사 이미지 출처는 유지한다. TypeScript 검사, `git diff --check`, AIT build가 성공했고 deploymentId는 `019f54e1-f528-7530-af86-609f9b571a79`이다.
- 2026-07-12 KST 저장 검증: SNS용 저장 캔버스 `1080x1350`은 유지하고 내부 카드 폭을 `888px`에서 `810px`로 줄여 좌우 `135px` 여백으로 중앙 정렬했다. SVG 저장 시 지원되지 않던 우측 끝 정렬을 제거해 사진 출처가 카드 밖으로 밀리는 현상을 막고, 위치/AI 근거 줄바꿈 폭과 구분자를 조정해 줄 첫머리에 점만 남지 않게 했다. TypeScript 검사, `git diff --check`, AIT build가 성공했고 deploymentId는 `019f54eb-1e6a-71d9-a47b-c2329d3a06f7`이다.
- 2026-07-12 KST 저장 검증: 익명 추천 QC 저장/90일 보존/RLS, 일간·주간 Codex QC 자동화, 서버 7문항 채택, 동굴·산사·테마파크·루지 인텐트를 반영했다. 실제 KTO 검색 결과는 첫 페이지 기준 10/20/10/9건이었다. 서버 리소스와 앱 질문은행 SHA-256이 일치했고 14그룹/420문항 및 런타임 7문항을 유지했다. `jbg` Wherego/QC unittest 48개, TypeScript, 질문 probe, `git diff --check`, AIT build가 성공했고 deploymentId는 `019f55cd-b4e1-7a76-bd40-b840763dc74b`이다.
- 2026-07-12 KST 프로덕션 검증: Render 질문은행 버전/7문항, 후보 `8 -> 7 -> 5`, Gemini 3.1 Flash Lite, 3201ms, 이미지·지도 포함 결과와 QC DB `7/3/4` 저장을 확인했다. smoke 레코드는 검증 후 삭제했다. Vercel 배포 `dpl_AZ1p4nVd9TJif3c4BL5ZUxfn3LTW`는 READY이며 production alias는 `https://wherego-lake.vercel.app`이다.

```text
https://wherego-lake.vercel.app
https://wherego-lake.vercel.app/terms/service
https://wherego-lake.vercel.app/terms/privacy
```

약관 정적 빌드:

```powershell
& 'C:\Users\ESOL\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' scripts/build-vercel-terms.cjs
```

성공 시:

```text
Built static terms pages for Vercel.
```

생성물:

```text
.vercel/output/static/index.html
.vercel/output/static/terms/service/index.html
.vercel/output/static/terms/privacy/index.html
```

질문풀/API 추천 플로우:

```powershell
$env:WHEREGO_PROBE_TODAY='2026-07-09'
& 'C:\Users\ESOL\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' scripts/probe-question-bank-result.cjs
Remove-Item Env:\WHEREGO_PROBE_TODAY
```

최근 확인 결과:

```text
출발지: 서울시청 테스트 위치
조건: 아이와 가족, 근교, 숲/수목원, 한산함, 주차 후 짧은 보행
추천: 일월수목원, 일영허브랜드(숲길정원), 무릉도원수목원
혼잡도 데이터 최신 기준일: 20260609
```

질문지 구조 검증:

```powershell
& 'C:\Users\ESOL\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --check scripts/build-general-question-bank.cjs
& 'C:\Users\ESOL\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' --check scripts/probe-question-bank-result.cjs
```

최근 확인 결과:

```text
movement_scope: 6 variants, select_2 1개, select_4 5개
party_constraints: 7 variants, select_2 2개, select_4 5개
destination_intent: 5 variants, select_2 2개, select_4 3개
general question bank: 13 tagGroups, 390 questions, source options 12 per group
```

## 자산 검증

- `assets/logo.png`는 600x600이어야 한다.
- `assets/wherego-logo-600.png`는 산·바다·파라솔을 포함한 업로드용 600x600 최종 로고 후보이며 앱 코드에는 자동 연결하지 않는다.
- `assets/thumbnail.png`는 1932x828이어야 한다.
- 썸네일 문구가 비주얼과 겹치거나 잘리지 않아야 한다.
- 질문 선택 목업은 `public/mockups/question-flow/index.html`에서 확인한다.
- 질문 화면은 이미지 없이 카드만 사용한다.

## 앱 검증

- TypeScript typecheck
- Granite build
- Apps in Toss dev server 실행
- 질문 선택 흐름
- 결과 카드 렌더링
- 지도 앱 링크 동작
- API 키가 번들에 포함되지 않는지 확인
- Apps in Toss 실기기에서 `saveBase64Data` PNG 카드 저장과 미지원 버전 저장 불가 메시지 확인
- Android 샌드박스 dev가 깨질 경우 `.yarn/patches`의 Granite/Metro/React Native patch 적용 여부를 먼저 확인
- 2026-07-12 KST 추천 횟수 기능 검증: KST 기본 3회, 리워드 광고 +1회(하루 최대 10회), 친구 공유 +3회(하루 1회)의 서버 예약·확정·환불·멱등 정책을 추가했다. 백엔드 Wherego unittest 51개, `yarn typecheck`, `git diff --check`, AIT Android/iOS 번들이 통과했고 공유 리워드 ID 반영 후 `wherego.ait` deploymentId는 `019f563a-5b8e-7271-a1f4-120e83da8183`이다. 공유 리워드 모듈 ID는 `1e6b212b-9093-4546-9991-99f478262910`이다.
- 2026-07-12 KST 프로덕션 사용량 스모크: Render가 `ff65b8a8a503645193bac5007cd1f68a5dc3f7f2`를 서비스하며 `/api/wherego/usage`가 기본 한도/잔여 `3/3`, 광고 보상 `0`, 공유 미사용, KST 자정 초기화 시각을 정상 반환했다.
- 2026-07-13 KST 광고 정책 검증: 기본·리워드 광고·공유로 받은 추천 횟수 모두 결과 전면광고를 거치도록 통일했다. 리워드 광고는 횟수 충전에만 사용하며 보상 지급과 광고 종료 후 첫 화면으로 자동 복귀한다. `yarn typecheck`와 AIT Android/iOS 빌드가 통과했고 deploymentId는 `019f5778-7420-7555-ae22-aa499cda7567`이다.
- 2026-07-13 KST 뒤로가기 검증: `useBackEvent`로 내부 화면은 첫 화면 복귀, 첫 화면은 `계속하기 / 나가기` 확인 후 `closeView` 호출로 변경했다. `yarn typecheck`와 AIT Android/iOS 빌드가 통과했고 deploymentId는 `019f577c-5315-7250-9776-f8154e2446bc`이다.
